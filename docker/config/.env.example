MI_PORT=8080
B2B_SIP_PORT=5060

# Optional: Set advertised IP address for SIP communication (default: localhost)
# For instance when running in Docker (in Colima) where external SIP device needs
# host IP to communicate properly.
ADVERTISED_IP=localhost
